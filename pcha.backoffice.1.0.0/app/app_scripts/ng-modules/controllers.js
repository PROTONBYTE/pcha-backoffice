'use strict';

/* App Directives  */

var appControllers = angular.module('appControllers', []);

appControllers.controller('ServiceCallsController', ['$scope',
    function($scope) {
        $scope.phones = 'test';
        $scope.orderProp = 'age';
    }]);

appController.controller('ServiceCallController', ['$scope',
    function($scope) {
    
    }]);

appController.controller('CreateServiceCallController',['$scope', 
    function($scope) { 
    
    }]);